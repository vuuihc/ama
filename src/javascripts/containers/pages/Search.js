import SearchBar from "../SearchBar.js"
import SearchResult from "../SearchResult.js"

const Search = ({
    // questions,
    // tutors
}) => {
    return (
        <div>
            <SearchResult />
        </div>

    )
}

export default Search
